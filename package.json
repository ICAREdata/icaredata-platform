{
  "name": "icaredata",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha test --timeout 30000",
    "clean": "rm -rf terraform/build && (cd terraform && terraform destroy <<< yes)",
    "build-message": "mkdir -p terraform/build && zip -qr terraform/build/process-message.zip process-message/ node_modules/ utils/",
    "build-conformance": "mkdir -p terraform/build && zip -qr terraform/build/conformance.zip conformance/",
    "build-bulkdata": "mkdir -p terraform/build && zip -qr terraform/build/bulkdata.zip bulkdata/",
    "build-all": "npm run build-message && npm run build-conformance && npm run build-bulkdata",
    "clean-build": "npm run clean && npm run build-all",
    "deploy": "(cd terraform && terraform init && terraform apply <<< yes)",
    "build-deploy": "npm run build-all && npm run deploy",
    "clean-deploy": "npm run clean && npm run build-deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.594.0",
    "fhirpath": "^0.17.4",
    "fs": "0.0.1-security",
    "knex": "^0.19.5",
    "lodash": "^4.17.15",
    "pg": "^7.18.1"
  },
  "devDependencies": {
    "axios": "^0.19.0",
    "chai": "^4.2.0",
    "eslint": "^6.6.0",
    "eslint-config-google": "^0.14.0",
    "mocha": "^6.2.2",
    "node-jose": "^1.1.3",
    "grunt": "^1.0.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ICAREdata/icaredata-platform"
  }
}
